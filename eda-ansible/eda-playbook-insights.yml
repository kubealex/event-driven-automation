---
- name: Triggering playbook for insights events
  hosts: localhost
  tasks:
    - name: Debug message
      ansible.builtin.debug:
        msg:
          - "Event type: {{ event.payload.event_type }}"
